---
// Circular terracotta-tinted badge holding a line icon (contact block).
const { class: extra = "" } = Astro.props;
---

<div
  class:list={[
    "flex h-12 w-12 items-center justify-center rounded-full bg-primary/10 text-primary",
    extra,
  ]}
>
  <slot />
</div>
