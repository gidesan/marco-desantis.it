---
// Small tracked uppercase label above headings ("Esplora", "L'Autore", ...).
const { class: extra = "" } = Astro.props;
---

<p
  class:list={[
    "font-body text-sm tracking-[0.3em] uppercase text-muted-foreground",
    extra,
  ]}
>
  <slot />
</p>
